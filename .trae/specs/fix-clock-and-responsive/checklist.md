# 验证清单

- [x] `app.js` 中 `updateAnalog()` 有元素判空守卫
- [x] `clock.js` 中 IIFE 有 `#clock` 判空守卫
- [x] 数字时间每秒更新不中断
- [x] `:root` 中无 `font-size: calc(1vw + 1vh)` 等缩放
- [x] 所有组件尺寸使用 `clamp()` / `vw` / `%` / `max-width` 等现代单位
- [x] 大屏（≥800px）布局正确：两卡片并排
- [x] 小屏（<800px）布局正确：卡片纵向排列
- [x] 名单增删改功能正常
- [x] 吃饭弹窗功能正常
- [x] 控制台无异常错误
