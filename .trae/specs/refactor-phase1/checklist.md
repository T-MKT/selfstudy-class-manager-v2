# 阶段一验证清单

- [x] CSS 中 `transition` 属性使用 `opacity` 而非 `opcity`
- [x] `js/alarm.js` 中不存在 `while (!ready) {}` 语句
- [x] `js/min.js` 中不存在 `Ready()` 函数
- [x] `js/time.js` 中不存在 `Ready()` 调用
- [x] `js/data.js` 中不存在 `ready` 变量
- [x] `js/alarm.js` 的 IIFE 已改为 `DOMContentLoaded` 事件驱动
- [x] `js/name.js` 使用 `event.key === 'Enter'` 而非 `event.keyCode === 13`
- [x] 所有文件无语法错误，页面可正常加载
